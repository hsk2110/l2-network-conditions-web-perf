
Stability Responsiveness
- Stability
    - ul_stable_throughput higher when stable
- Numeric Values
    - dl_throughput is slightly lower when stable
    - dl_rpm_p is slightly higher when stable
    - dl_rpm_trimmed is higher when stable
    - ul_throughput is slightly higher when stable
    - ul_rpm_p is slightly lower when stable
    - ul_rpm_trimmed is slightly lower when stable
    - final_rpm_p is lower when stable
    - final_rpm_trimmed is lower when stable
    - less wc_effect when stable

Stability Throughput
- Stability
    - They are the same
- Numeric Values
    - dl_rpm_p is higher when stable
    - dl_rpm_trimmed too
    - ul_rpm_p is pretty much the same
    - ul_rpm_trimmed too
    - both final rpms are lower
    - wc_effect is lower when stables

Summary:
for upload and download rpms its unclear how stability affects them
but for final rpm, it tends to overestimate them, if unstable

in networkQuality.go line 1257:
	// Now, if the test had a stable responsiveness measurement, then only consider the
	// probe measurements that are in the MAD intervals. On the other hand, if the test
	// did not stabilize, use all measurements to calculate the RPM.
